# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`themis` is a Rust project (edition 2024) in early development, currently containing a stub `Account` struct with a placeholder `main` and one test.

## Commands

```bash
cargo build          # compile
cargo run            # build and run
cargo test           # run all tests
cargo test test_deposit  # run a single test by name
cargo clippy         # lint
cargo fmt            # format
```
